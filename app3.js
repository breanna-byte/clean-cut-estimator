function renderCustomers(){el('adminContent').innerHTML=`<h2>Customers</h2>${db.customers.length?`<div class="tableWrap"><table><thead><tr><th>Name</th><th>Contact</th><th>Location</th><th>Last activity</th></tr></thead><tbody>${db.customers.map(c=>`<tr><td><b>${esc(c.name)}</b></td><td>${esc(c.phone)}<br>${esc(c.email)}</td><td>${esc(c.address||'')} ${esc(c.city||'')}</td><td>${new Date(c.lastContact).toLocaleDateString()}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">Customers are added from saved quote requests.</div>'}`}
function renderCalendar(){const now=new Date(),y=now.getFullYear(),m=now.getMonth(),first=new Date(y,m,1),start=new Date(y,m,1-first.getDay());let days='';for(let i=0;i<42;i++){const d=new Date(start);d.setDate(start.getDate()+i);const ds=d.toISOString().slice(0,10),events=db.jobs.filter(j=>j.date===ds);days+=`<div class="day"><b>${d.getDate()}</b>${events.map(e=>`<span class="event">${esc(e.customer)}</span>`).join('')}</div>`}el('adminContent').innerHTML=`<h2>${now.toLocaleString('default',{month:'long'})} ${y}</h2><div class="calendar">${days}</div>`}
function numberField(id,label,value,step='0.01'){return`<div class="field"><label>${label}</label><input id="${id}" type="number" step="${step}" value="${value}"></div>`}